// The only thing the static site needs to know about the backend.
// Change this to the Render URL when deploying; localhost is the default for development.
window.NEON_API_BASE = "http://127.0.0.1:8000";
